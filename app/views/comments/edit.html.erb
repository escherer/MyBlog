<h1>Edit Your Comment</h1>

<%= form_for [@post, @comment] do |f| %>
	<%= f.text_area :comment_content %><br />
	<%= f.hidden_field :post_id %>
	<%= f.submit 'Submit Comment' %>
<% end %>

<%= link_to 'cancel', posts_path %>